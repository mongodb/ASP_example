# Copilot instructions for asp_example
- Prefer $source.connectionName over embedding URIs in code.
- When writing new processors, mirror the quick‑start file naming pattern.
- Use sp.process() for demos; sp.create()+start() for long‑running jobs.
- Use the extension .js or .mongodb for files. The .mongodb extension works for the MongoDB VS Code plugin.
